{
  "name": "mattcook.dev",
  "description": "Matt Cook – Software Engineer Portfolio",
  "private": true,
  "version": "0.1.0",
  "author": "Matt Cook <matt@sciencefidelity.co.uk>",
  "repository": {
    "type": "git",
    "url": "git@github.com:sciencefidelity/mattcook.dev.git"
  },
  "homepage": "https://mattcook.dev",
  "keywords": [
    "sanity",
    "nextjs"
  ],
  "scripts": {
    "dev": "pnpm run --parallel -r dev",
    "build": "pnpm run -r build",
    "prestart": "pnpm run build",
    "start": "pnpm run build && pnpm run --parallel -r start",
    "test": "pnpm run -r test",
    "deploy": "pnpm run -r deploy",
    "test:watch": "pnpm run -r --parallel test:watch",
    "studio:build": "pnpm run --filter studio build",
    "studio:login": "pnpm run --filter studio login",
    "studio:deploy": "pnpm run --filter studio deploy",
    "studio:test": "pnpm run --filter studio test",
    "studio:test:wach": "pnpm run --filter studio test:watch",
    "web:dev": "pnpm run --filter web dev",
    "web:start": "pnpm run --filter web dev",
    "web:build": "pnpm run --filter web build",
    "web:test": "pnpm run --filter web test",
    "web:test:watch": "pnpm run --filter web test:watch",
    "format": "pnpm run -r format",
    "lint": "pnpm run -r lint",
    "fix": "pnpm run -r fix"
  }
}
